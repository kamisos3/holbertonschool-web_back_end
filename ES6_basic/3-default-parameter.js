export default function getSumOfHoods(initialNumber, expansion1989 = 89, expansion2019 = 19) {
  // This condenses a function block (simplifies)

  return initialNumber + expansion1989 + expansion2019;
}
